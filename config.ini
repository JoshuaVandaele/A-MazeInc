return {
  resizeCMD = true,    --If the size of the window changes or not
  sprites = "sprites", --File that contains sprites (MUST BE A .lua FILE.)
  mazeDir = "mazes",   --Directory of the directory where maze files will be located
  mazeExt = ".maze",   --Extention of maze files
}